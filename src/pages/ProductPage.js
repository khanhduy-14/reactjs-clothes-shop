import React from "react";
import { useSelector } from "react-redux";

const ProductPage = () => {
  // const { data: product, loading } = useSelector((state) => state.productState)
  // console.log(product);
  return (
    <div className="bg-gradient-to-r from-violet-500 to-fuchsia-500 h-[660px]">
      <div className="grid grid-cols-4 gap-10">hihiigaga</div>
    </div>
  );
};

export default ProductPage;
